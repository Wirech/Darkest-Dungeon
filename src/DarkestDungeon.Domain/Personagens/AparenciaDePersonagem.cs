namespace DarkestDungeon.Domain.Personagens;

/// Variante visual (paleta) do Personagem — A, B, C ou D.
/// Cada `Classe` expõe os assets Spine correspondentes para cada aparência via `AssetsDeClasse`.
public enum AparenciaDePersonagem
{
    A = 0,
    B = 1,
    C = 2,
    D = 3,
}
